#include <stdio.h>

int main(){
    int a = 10; // Unary plus
    int b = -a; // Unary minus
    int c = ++a; // Increment
    int d = --a; // Decrement 
    printf("Nilai a: %d, b: %d, c: %d, d: %d\n", a, b, c, d);
}